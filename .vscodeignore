# source
src
scripts
assets
!assets/logo.png
!assets/patchwalk-activitybar.svg

# output — ship the two esbuild bundles the extension host + spawned daemon run from
# (out/src/extension/index.js and out/src/daemon/index.js), but nothing else under out/.
out/test
out/**/*.map
out/**/*.d.ts
out/**/*.tsbuildinfo
*.vsix

# test
test
test-workspace-primary
test-workspace-secondary
.vscode-test

# configs
.github
.vscode
tsconfig.*
.eslintrc*
.prettier*
.gitignore
.gitattributes
.nvmrc
.npmrc
pnpm-lock.yaml

# dev-only docs + local/agent dirs (never shipped)
PLAN.md
BUILD_PROGRESS.md
_local
.codex
.claude

# esbuild-visualizer
meta.json
stats.html
